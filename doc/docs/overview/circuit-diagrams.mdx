---
sidebar_position: 3
---

import Ldo78l05Diagram from '../_fragments/ldo-78l05-diagram.mdx';

# Circuit Diagrams

Circuit diagrams for the zudo-bus board.

## System Block Diagram

```
                           zudo-bus Board
┌──────────────────────────────────────────────────────────────────────┐
│                                                                      │
│   INPUTS                    PROCESSING              OUTPUTS          │
│                                                                      │
│  From PSU                                          To Modules        │
│  ┌──────────────┐                                 ┌──────────────┐   │
│  │ FASTON x4    │──┐     ┌──────────────┐    ┌───►│ IDC J1 (16P) │   │
│  │ +12V,-12V    │  │     │              │    │    ├──────────────┤   │
│  │ +5V, GND     │  │     │  POWER RAIL  │    │    │ IDC J2 (16P) │   │
│  └──────────────┘  ├────►│  DISTRIBUTION│────┼───►├──────────────┤   │
│         OR         │     │              │    │    │     ...      │   │
│  ┌──────────────┐  │     └──────────────┘    │    ├──────────────┤   │
│  │ Screw x4     │──┘            │            └───►│ IDC J8 (16P) │   │
│  │ 5.08mm      │                │                 └──────────────┘   │
│  └──────────────┘                │                                    │
│                                  ▼                                    │
│                    ┌───────────────────────┐                         │
│                    │   +5V LDO SECTION     │                         │
│                    │                       │                         │
│                    │  +12V ─► AMS1117 ─┐   │                         │
│                    │                   │   │                         │
│                    │  PSU +5V ─────────┼───┼─► 3-pin JP1             │
│                    │                   │   │      │                  │
│                    │  LDO +5V ─────────┘   │      ▼                  │
│                    └───────────────────────┘   +5V Rail              │
│                                                                      │
│  PROTECTION (Order: PTC → TVS → Diode → Caps)    INDICATORS         │
│  ┌─────────────────────────────────────────────┐ ┌─────────────┐    │
│  │ PTC Fuses: F1(+12V), F2(-12V), F3(+5V),     │ │  LED1 -12V  │    │
│  │            F4(LDO input)                    │ │  LED2 +12V  │    │
│  │ TVS Clamp: TVS1(+12V), TVS2(-12V),          │ │  LED3 +5V   │    │
│  │            TVS3(+5V) - SMF12CA for ±12V     │ └─────────────┘    │
│  │ Reverse: D1(+12V), D2(-12V),                │                    │
│  │          D3(+5V PSU), D4(+5V LDO)           │                    │
│  │ OR-ing: D5(+5V PSU), D6(+5V LDO) - SS14     │                    │
│  └─────────────────────────────────────────────┘ DECOUPLING         │
│                                                 ┌─────────────┐     │
│  TEST POINTS                                    │ C5,C6: bulk │     │
│  ┌─────────────────────────────────┐            │ C7-C14:+12V │     │
│  │ TP1:+12V  TP2:-12V  TP3:+5V     │            │ C15-C22:-12V│     │
│  │ TP4:GND   TP5:LDO in TP6:LDO out│            │ per header  │     │
│  └─────────────────────────────────┘            └─────────────┘     │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

## +5V LDO Circuit

<Ldo78l05Diagram />

## LED Indicator Circuit

```
+12V Rail                                                +5V Rail
    │                                                        │
    │                                                        │
    ▼                                                        ▼
   R2                                                       R3
  1kΩ                                                      1kΩ
    │                                                        │
    ▼                                                        ▼
 ┌─────┐                                                 ┌─────┐
 │LED2 │                                                 │LED3 │
 │Green│                                                 │Blue │
 │A → K│                                                 │A → K│
 └──┬──┘                                                 └──┬──┘
    │                                                        │
    ▼                                                        ▼
   GND                                                      GND


LED1 (-12V Indicator) - Different topology:

   GND (0V)
    │
    ▼
 ┌─────┐
 │LED1 │  Current flows from GND (higher potential)
 │ Red │  to -12V (lower potential)
 │A → K│
 └──┬──┘
    │
    ▼
   R1
  1kΩ
    │
    ▼
 -12V Rail
```

**LED Current (all 1kΩ resistors, same as zudo-power-usb-pd):**

- LED1 (Red, -12V): I = (0V - (-12V) - 2V) / 1kΩ ≈ 10mA
- LED2 (Green, +12V): I = (12V - 2V) / 1kΩ ≈ 10mA
- LED3 (Blue, +5V): I = (5V - 3V) / 1kΩ ≈ 2mA

**Note:** For LED1 (-12V indicator), the LED anode connects to GND (0V) and current flows toward the -12V rail. This is the reverse of the +12V and +5V LEDs where current flows from the positive rail to GND.

## Reverse Polarity Protection

```
+12V Rail                              -12V Rail
    │                                      │
    │                                      │
    ├──────────────┐          ┌────────────┤
    │              │          │            │
    │          Cathode    Anode            │
    │            ┌─┴─┐    ┌─┴─┐            │
    │            │ D1│    │ D2│            │
    │            │   │    │   │            │
    │            └─┬─┘    └─┬─┘            │
    │          Anode    Cathode            │
    │              │          │            │
    │              └────┬─────┘            │
    │                   │                  │
    │                  GND                 │
    │                                      │
    ▼                                      ▼
To IDC Headers                      To IDC Headers

D1, D2, D3, D4: SM4007PL (1N4007 equivalent, SOD-123FL)
- Clamps reverse voltage to ~0.7V
- Protects modules from reversed power cable
- D3: PSU +5V input path protection
- D4: LDO +5V output path protection
```

## +5V OR-ing Circuit (Backfeed Protection)

The +5V rail has two possible sources: the on-board LDO and the external PSU. Schottky diodes D5 and D6 form an OR-ing configuration to prevent backfeed between sources.

```
                    +5V Source Selection with OR-ing Protection

+12V Rail ──► F4 ──► AMS1117 ──► D6 (SS14) ──┐
             PTC     LDO         Schottky    │
            1.1A    +5V out      cathode     │
                                             ├──► JP1 Pin 1
                                             │    (LDO source)
                                             │
                                             │        ┌───┐
PSU +5V ──► D3 ──► D5 (SS14) ────────────────┼──────► │ 1 │ LDO +5V
            SM4007  Schottky                 │        │   │
            reverse cathode                  │        │ 2 │ +5V Rail ──► F3 ──► Distribution
            protect                          │        │   │
                                             └──────► │ 3 │ PSU +5V
                                                      └───┘
                                                       JP1
Benefits:
- Prevents LDO damage if both sources connected simultaneously
- Allows safe operation even with incorrect jumper configuration
- ~0.55V drop through Schottky (lower than silicon diode)

D5, D6: SS14 (40V, 1A Schottky, SMA package)
- Forward voltage: 550mV @ 1A
- Reverse voltage: 40V
- Peak surge current: 25A
```

## Power Sequencing

```
Power-On Sequence (Natural):

1. PSU provides +12V, -12V, (optionally +5V) simultaneously
2. ±12V rails stabilize quickly (bulk caps charge)
3. If using LDO mode (JP1 position 1-2):
   - +5V rail comes up AFTER +12V stabilizes
   - AMS1117 requires stable input before output regulation
   - This provides natural sequencing: ±12V before +5V
4. If using PSU direct mode (JP1 position 2-3):
   - All rails come up simultaneously

Power-Off Sequence:
- All rails fall together when PSU is disconnected
- Bulk capacitors provide brief hold-up time

Note: The natural sequencing in LDO mode (±12V before +5V) is beneficial
for many digital modules that expect analog supplies before digital.
```

## Protection Circuit Order

```
Input → PTC Fuse → TVS Diode → Reverse Diode → Bulk Caps → Distribution

+12V Rail Example:
                                                              To IDC
Input ──► F1 (2A PTC) ──► TVS1 (12V) ──► D1 (reverse) ──► C5 (22µF) ──► Headers
          overcurrent     transient      polarity         bulk
          protection      clamp          protection       filtering
                          (SMF12CA)
                          clamps@19.9V
```

## TVS Transient Protection

```
+12V Rail              -12V Rail              +5V Rail
    │                      │                      │
    │                      │                      │
   TVS1                   TVS2                   TVS3
  SMF12CA               SMF12CA                SMF5.0CA
  (12V bi)              (12V bi)              (5V bi)
    │                      │                      │
   GND                    GND                    GND

Specifications:
- SMF12CA: 12V standoff, clamps to 19.9V max (safe for module op-amps)
- SMF5.0CA: 5V standoff, clamps to 9.2V max
- Bidirectional - protects against positive and negative spikes

Design Note: SMF12CA chosen over SMF15CA because 24.4V clamping exceeds
the absolute maximum ratings of many Eurorack module op-amps (15-18V).
```

## PTC Overcurrent Protection

```
+12V Input ──► F1 ──► +12V Rail
              BSMD1812-200 (2A hold, 4A trip)

-12V Input ──► F2 ──► -12V Rail
              BSMD1812-200 (2A hold, 4A trip)

+12V Rail ──► F4 ──► LDO Input (U1 Pin 3)
              BSMD1812-110 (1.1A hold, 2.2A trip)

JP1 Pin 2 ──► F3 ──► +5V Distribution ──► To IDC Headers
(after jumper) BSMD1812-150 (1.5A hold, 3A trip)

Notes:
- Self-resetting: auto-recovers after fault clears
- Protects against short circuits and overcurrent
- F3 is placed AFTER JP1 Pin 2, protecting +5V distribution
  regardless of source selection (LDO or PSU direct)
- F4 provides symmetric protection for LDO input path,
  limiting current if LDO shorts internally
```

## Per-Header Decoupling

Each IDC header has dedicated decoupling capacitors for +12V and -12V rails.

```
+12V Rail Decoupling (per header):

+12V Rail ──┬────────┬────────┬─── ... ───┬─── To IDC Headers
            │        │        │           │
           C7       C8       C9         C14
         0.1µF    0.1µF    0.1µF      0.1µF
        ceramic  ceramic  ceramic    ceramic
            │        │        │           │
           GND      GND      GND         GND
          (J1)     (J2)     (J3)       (J8)

-12V Rail Decoupling (per header):

-12V Rail ──┬────────┬────────┬─── ... ───┬─── To IDC Headers
            │        │        │           │
          C15      C16      C17         C22
         0.1µF    0.1µF    0.1µF      0.1µF
        ceramic  ceramic  ceramic    ceramic
            │        │        │           │
           GND      GND      GND         GND
          (J1)     (J2)     (J3)       (J8)
```

**Capacitor Assignment:**

| Header | +12V Cap | -12V Cap |
| ------ | -------- | -------- |
| J1     | C7       | C15      |
| J2     | C8       | C16      |
| J3     | C9       | C17      |
| J4     | C10      | C18      |
| J5     | C11      | C19      |
| J6     | C12      | C20      |
| J7     | C13      | C21      |
| J8     | C14      | C22      |

**Purpose:**

- High-frequency noise filtering at each output
- Prevents noise coupling between modules
- Local energy storage for transient loads
- Place capacitors as close to IDC header pins as possible

## 16-Pin IDC Pinout (Eurorack Standard)

```
┌─────────────────────────────────┐
│    Eurorack 16-Pin Header       │
│         (Top View)              │
│                                 │
│   Pin 1  ●───────● Pin 2        │
│   -12V         -12V             │
│                                 │
│   Pin 3  ●───────● Pin 4        │
│   GND           GND             │
│                                 │
│   Pin 5  ●───────● Pin 6        │
│   GND           GND             │
│                                 │
│   Pin 7  ●───────● Pin 8        │
│   GND           GND             │
│                                 │
│   Pin 9  ●───────● Pin 10       │
│   +12V         +12V             │
│                                 │
│   Pin 11 ●───────● Pin 12       │
│   +12V         +12V             │
│                                 │
│   Pin 13 ●───────● Pin 14       │
│   +5V          +5V              │
│                                 │
│   Pin 15 ●───────● Pin 16       │
│   CV           GATE             │
│   (NC)         (NC)             │
│                                 │
│         ▲ Keying notch          │
└─────────────────────────────────┘

Pin Functions:
- Pins 1-2:    -12V power
- Pins 3-8:    Ground (x6)
- Pins 9-12:   +12V power (x4)
- Pins 13-14:  +5V power (x2)
- Pins 15-16:  CV/Gate (typically NC on bus boards)
```

## Test Points

```
┌────────────────────────────────────────────────────────────────┐
│                       Test Point Locations                      │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  TP1 (+12V) ────── After PTC F1, main +12V rail               │
│  TP2 (-12V) ────── After PTC F2, main -12V rail               │
│  TP3 (+5V)  ────── After jumper JP1, +5V to modules           │
│  TP4 (GND)  ────── Ground reference for measurements          │
│  TP5 (LDO in) ─── +12V input to LDO U1                        │
│  TP6 (LDO out) ── +5V output from LDO U1 (before JP1)         │
│                                                                │
└────────────────────────────────────────────────────────────────┘

Usage:
- Use TP4 (GND) as reference for all voltage measurements
- TP5/TP6 useful for diagnosing LDO issues
- TP1-TP3 verify protection circuits are passing power correctly
```

## See Also

- [Technical Overview](./overview.md) - Design rationale
- [Bill of Materials](./bom.md) - Component list
- [Current Status](../inbox/current-status.md) - Detailed connection list
